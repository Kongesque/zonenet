# Backend .env example
# Copy to .env for configuration

# JWT Secret Key (REQUIRED - generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=your-secure-secret-key-here

# Frontend URL for CORS (change for production)
FRONTEND_URL=http://localhost:3000

# Enable debug mode (enables /docs endpoint, disable in production)
DEBUG=true

# Password reset (set true, restart, check logs for new password)
# RESET_PASSWORD=true
